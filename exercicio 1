#include <stdio.h>
int main() {
int numeros[10];
    
    printf("Digite 10 números inteiros:");
    for (int i = 0; i < 10; i++) {

    printf ("numero %d:" , i+1);
    scanf ("%d", &numero [i]);
    
    printf("Os numeros são");
    for (int i = 0; i < 10; i++) {
        printf("%d ", numeros[i]);
    }
    printf("\n");
    
    return 0;
}
