#include <stdio.h>
int main() {
int vetor[8];
int i;
    
    printf("Digite 8 elementos:");
    for (int i = 0; i < 8; i++) {

     printf ("numero %d", i+1);
     scanf("%d", &vetor[i]);
    
    printf("os numeros são");
    for (int i = 7; i >= 0; i--) {
        printf("%d ", vetor[i]);
    }
    printf("\n");
    
    return 0;
}
